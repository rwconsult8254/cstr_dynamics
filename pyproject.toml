[project]
name = "cstr-dynamics"
version = "0.1.0"
description = "Real-time CSTR dynamics simulator with temperature control"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
]
